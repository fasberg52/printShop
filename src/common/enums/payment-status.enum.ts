export enum PaymentStatus {
  CREATED = 'created',
  STARTED = 'started',
  PENDING = 'pending',
  SUCCESS = 'success',
  FAILED = 'failed',
  REFUNDED = 'refunded',
  CANCELED = 'canceled',
  FAILED_DELAYED = 'failed_delayed',
}